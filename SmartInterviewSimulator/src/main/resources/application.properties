spring.application.name=SmartInterviewSimulator
# ===============================
# SERVER CONFIG
# ===============================
server.port=${PORT:8080}

# ===============================
# DATABASE CONFIG (MySQL)

# ===============================

spring.datasource.url=${SPRING_DATASOURCE_URL:${DATABASE_URL:${MYSQL_URL:jdbc:mysql://localhost:3306/interview_db?createDatabaseIfNotExist=true}}}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:${DATABASE_USERNAME:${MYSQL_USER:root}}}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:${DATABASE_PASSWORD:${MYSQL_PASSWORD:root2003}}}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# JWT CONFIG
# ===============================

jwt.expiration=3600000



groq.api.key=${GROQ_API_KEY:YOUR_GROQ_API_KEY}
groq.url=https://api.groq.com/openai/v1/chat/completions

# ===============================
# OPENAI CONFIG
# ===============================
gemini.api.key=${GEMINI_API_KEY:YOUR_GEMINI_API_KEY}
gemini.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent
# ===============================
# LOGGING (OPTIONAL)
# ===============================
logging.level.org.springframework=INFO
logging.level.com.interview.simulator=DEBUG
